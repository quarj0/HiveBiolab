### HiveBiolab Django backend environment configuration
### Copy this file to .env and keep it out of version control.

# Django defaults
DJANGO_SECRET_KEY=changeme
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=localhost 127.0.0.1

# Frontend origins for CORS/CSRF; space-separate multiple origins
FRONTEND_ORIGINS=http://localhost:5173
CSRF_TRUSTED_ORIGINS=http://localhost:5173

# Firebase credentials (local only; Cloud Run can rely on its service account)
# By default we expect `kumasihivewebsite-service.json` inside the project root.
GOOGLE_APPLICATION_CREDENTIALS=kumasihivewebsite-service.json

# Jazzmin branding overrides
JAZZMIN_SITE_TITLE=HiveBiolab Admin
JAZZMIN_SITE_HEADER=HiveBiolab Team
JAZZMIN_SITE_BRAND=HiveBiolab
JAZZMIN_WELCOME_SIGN=Review newsletter, contact, and training requests
JAZZMIN_COPYRIGHT=HiveBiolab © 2025
